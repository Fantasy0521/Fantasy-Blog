# linux环境下的配置

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/fantasy-blog?useUnicode=true&zeroDateTimeBehavior=convertToNull&characterEncoding=utf-8&useSSL=false
    username: root
    password: mhs-mysql4001051166

# 静态文件上传访问路径 Linux环境如 :/home/My Blog/upload
upload:
  path: /usr/local/fantasy-blog/upload
  access:
    path: /images/**
  resources:
    locations: file:${upload.path}


